# Advent of Code

My solutions for Advent of Code, implemented in Go.

## Structure

Each day's solution is located in its own folder (e.g., `day01`, `day02`).

## Usage

To run a specific day's solution:

```bash
cd dayXX
go run main.go
```
